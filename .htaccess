# Redirect all traffic to public/ subdirectory
# Use this only if Document Root cannot be set to /public/ on Hostpoint
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
